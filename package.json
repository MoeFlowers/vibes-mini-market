{
  "name": "vibes-mini-market",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "api",
    "web",
    "shared"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=api",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspaces"
  }
}
